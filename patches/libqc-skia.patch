diff --git a/external/skia/Android.mk b/external/skia/Android.mk
index b32edab..f1c7fd1 100644
--- a/external/skia/Android.mk
+++ b/external/skia/Android.mk
@@ -263,9 +263,9 @@ LOCAL_STATIC_LIBRARIES := \
 	libwebp-decode \
 	libwebp-encode
 
-ifeq ($(BOARD_USES_QCOM_HARDWARE),true)
-	LOCAL_WHOLE_STATIC_LIBRARIES += libqc-skia
-endif
+#ifeq ($(BOARD_USES_QCOM_HARDWARE),true)
+#	LOCAL_WHOLE_STATIC_LIBRARIES += libqc-skia
+#endif
 
 
 LOCAL_C_INCLUDES += \
@@ -350,7 +350,7 @@ LOCAL_SRC_FILES:= \
 	src/gpu/GrPrintf_skia.cpp
 
 ifeq ($(BOARD_USES_QCOM_HARDWARE),true)
-	LOCAL_WHOLE_STATIC_LIBRARIES += libqc-skia
+	#LOCAL_WHOLE_STATIC_LIBRARIES += libqc-skia
 endif
 
 LOCAL_SHARED_LIBRARIES := \
